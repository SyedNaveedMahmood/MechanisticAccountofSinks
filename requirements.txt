--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.10.0+cu128
transformers==5.3.0
matplotlib==3.10.8
numpy==2.4.3
pandas==3.0.1
scikit-learn==1.8.0
scipy==1.17.1
tqdm==4.67.3
datasets==4.8.4
# E3 (emergence dynamics): checkpoint discovery + per-revision cache management.
# Floor rather than exact pin so it stays compatible with whatever transformers==5.3.0 resolves.
huggingface_hub>=0.34.0
